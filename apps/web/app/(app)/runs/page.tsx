import { PhaseStub } from "@/components/phase-stub";

export default function RunsPage() {
  return (
    <PhaseStub
      title="Runs"
      phase="Arrives in Phase 3"
      description="Live agent executions with model usage, token/cost totals, and sanitized tool-call timelines."
    />
  );
}
