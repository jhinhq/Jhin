import { PhaseStub } from "@/components/phase-stub";

export default function ModelsPage() {
  return (
    <PhaseStub
      title="Models"
      phase="Arrives in Phase 3"
      description="Model providers (OpenAI, Anthropic, OpenRouter, Ollama) and reusable model profiles with per-agent assignment."
    />
  );
}
