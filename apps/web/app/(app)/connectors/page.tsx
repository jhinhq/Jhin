import { PhaseStub } from "@/components/phase-stub";

export default function ConnectorsPage() {
  return (
    <PhaseStub
      title="Connectors"
      phase="Arrives in Phase 5"
      description="Authenticated integrations — GitHub, Linear, Vercel, Supabase, CLI, HTTP — with health, scopes, and per-agent access."
    />
  );
}
