import { PhaseStub } from "@/components/phase-stub";

export default function TriggersPage() {
  return (
    <PhaseStub
      title="Triggers"
      phase="Arrives in Phase 7"
      description="WHEN / IF / THEN automation: react to connector events and schedules by assigning work to agents or teams."
    />
  );
}
