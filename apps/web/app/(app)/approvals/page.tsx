import { PhaseStub } from "@/components/phase-stub";

export default function ApprovalsPage() {
  return (
    <PhaseStub
      title="Approvals"
      phase="Arrives in Phase 4"
      description="An inbox for high-risk agent actions awaiting human sign-off, backed by the policy engine and tool gateway."
    />
  );
}
