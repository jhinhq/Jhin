import { PhaseStub } from "@/components/phase-stub";

export default function TasksPage() {
  return (
    <PhaseStub
      title="Tasks"
      phase="Arrives in Phase 3"
      description="Durable, user-visible work items backed by Temporal workflows: manual assignment, timelines, and delegation trees."
    />
  );
}
