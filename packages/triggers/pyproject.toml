[project]
name = "jhin-triggers"
version = "0.1.0"
description = "Connector-agnostic trigger engine: safe JSON filter DSL, transition matching, deterministic idempotency keys (plan section 10, 9.4, 52)"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/jhin_triggers"]
