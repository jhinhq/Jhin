# Jhin accepts webhook request bodies up to exactly 1 MiB. The ingress
# EventEnvelope adds bounded metadata around those bytes, so the broker frame
# limit must be larger than the HTTP cap while remaining explicit and finite.
max_payload: 2097152
http_port: 8222

jetstream {
  store_dir: "/data"
}
