# Implementation ledger — 2026-09-11-agentic-chat-workspace

Specification: ../superpowers/plans/2026-09-11-agentic-chat-workspace.md and the user-approved conversation plan.

## Preflight

- Existing terminal internet/output changes are complete and remain the baseline.
- Checkout contains substantial pre-existing uncommitted work. Ruling: implement in this authorized shared checkout without Git mutations; isolate ownership by module and record changes here. Creating a clean worktree would omit the current baseline, and .git is read-only.
- All six first-release milestones are implemented and the final self-hosted acceptance workflow passed.

| Interface overlap | Resolution |
|---|---|
| File metadata / typed turns / composer | File worker publishes contract; runtime and UI consume IDs and versions |
| Timeline / all-tool rendering | Runtime worker owns backend; UI worker owns renderer |
| Chat disk / editor / PTY / previews | Root owns runtime binding and transport; file worker owns shared DB/API file layer |
| Migrations | 0045 files/workspaces, 0046 journal/turn state, 0047 sessions; all descend sequentially |
| API registration/scopes/lockfiles/deployment | Root-owned integration |

## Status

All six milestones are delivered and local release acceptance is complete. The main installation passed the saved-context → agent work → results → revision → restart recovery workflow; all four final authenticated browser scenarios passed against the isolated installation. The final main stack reports 12 healthy services. Evidence and the defects corrected during acceptance are recorded below.

- Final amended migration chain passes clean base→0047 and 0047→0044→0047 against disposable PostgreSQL. Journal commit-order/concurrency/rollback tests passed. The root integrator subsequently upgraded the local installation from 0044 to 0047 after backing up its database.
- Real Docker `scripts/verify_agentic_runner.py` passes contained read/write, revision conflict, two-chat isolation, PTY resize/replay/duplicate input/interrupt, internet curl, DOCX generation, retained disk after container removal. Uses uniquely named test volumes and disables only test object's startup reaper; no second runner server touches live jobs.
- `jhin-sandbox:agentic` image built: Node22, Python document libraries, websocket-client, LibreOffice.
- The final complete sandbox-runner + CLI suites pass against the shared source in a disposable Linux container: **668 passed, 5 skipped, 1 deselected**. Earlier Windows failures involved Unix-socket/transport platform requirements; the Linux run supplies the relevant platform evidence.
- The complete `packages/workflows/tests` Temporal suite passes: **154 passed** in 633 seconds. All **1,348 collected API tests** passed in the combined API/OpenAPI/release run. That combined run recorded **1,407 passes and 4 failures** in root boundary/platform checks and a stale console fixture. After correcting two test files, the focused rerun passed **12 tests with 2 Linux-only skips**, and an actual Linux boundary/documentation rerun passed **14 tests with no skips**, closing all four failures.
- Journal API and OpenAPI checks pass **43 tests**, including four new bounded loaded-item recovery cases. Both affected API source files pass strict mypy; targeted Ruff checks are clean.
- The final current-tree web suite passes **1,303 tests across 111 files** in **18.35 seconds**. Production build, TypeScript, ESLint, and mobile (390 px) / desktop (1440 px) checks pass. All four final authenticated browser cases passed together in **1.4 minutes**, including file editing and retention, PTY control, static/Vite/Next.js previews, HMR, HTTP POST, and actual browser WebSocket transport.
- Root wired runtime/file routes and scopes, scoped gateway, managed-files Docker volume, and dependency lock. The matching Docker images were built and the local Compose installation reports **12 healthy services** at the integration checkpoint.
- Fenced chat binding, isolated colleague copies, before-run checkpoints/output snapshots, legacy copy/import, session expiry, preview adapters, gateway revocation, retained project sources and storage accounting have targeted verification. Final runtime/preview/runner boundary checks passed **45 tests**; agent-focused checks passed **34 tests**, with targeted Ruff and strict mypy clean.

- [x] Files, projects and revisions backend
- [x] Durable conversation timeline, streaming and controls
- [x] Chat and workspace frontend
- [x] Chat disk binding, interactive terminals and previews
- [x] Final browser acceptance and resulting integration corrections
- [x] Complete release acceptance and local rollout sign-off

## Local upgrade and live workflow checkpoint

- The root integrator completed the local **0044→0047** upgrade. The pre-upgrade PostgreSQL dump is `.tmp/agentic-upgrade-backup/jhin-pre-agentic.dump`; its presence and **19,588,810-byte** size were independently checked. Keep this private database backup with the matching retained volumes; it is not a distributable fixture.
- The complete Docker build finished and the integrated local stack reports **12 healthy services**. This records the observed deployment checkpoint, not a guarantee about later service health.
- A live sample sales workflow completed in chat `01a093c9-c051-7c33-bfa9-8cfff7bf8de8`. Authenticated downloads passed: **report.docx v1**, 36,665 bytes, SHA-256 `475b8d404740989ea719292d446b0f98352a766525ab494e3f18f80d897a215c`; its document XML contains “Sales Report” and “Total sales: 60”. **results.csv v1** is 24 bytes and contains `metric,value` and `total,60`.
- A follow-up used saved report revision `01a093ce-646c-7cb0-8ac2-8aa59734c35c` explicitly pinned as input. It exposed a binary-read persistence defect, then completed through Python document tooling as a separate **report_revised.docx v1** artifact. After the fix, another live revision correctly published **report.docx v2** under the same file ID `01a093ce-646a-7f73-8bfe-a50e86aeeb02`, retaining pinned input v1. The 36,689-byte download has SHA-256 `af6cbe3984a41b166c86afc36c81312683d73d9a0d521032687a680f60d76c9c`; its XML retains total 60 and includes “Verified from the saved sales spreadsheet.” This closes the same-artifact immutable-revision check.
- API, tool worker, agent worker, runtime gateway, and runner were rebuilt with the live-check corrections after confirming they were idle. Restart recovery matched all **54 timeline items**, **6 file metadata records**, and saved file bytes exactly. SSE replay reconstructed the same **54 items** from **191 events**, including a forced disconnect at event 95 and cursor-based resume.
- Actual API review passed: edit `results.csv` from total 60 to 90, inspect the diff, reject a stale save with 409, reject a stale restore with 409, restore as immutable version 5, and branch with independently owned files. Saving project source and seeding a new chat also passed: the new chat has a different working directory and **5 persisted files**.
- Exact live command cancellation passed in sample chat `01a093e3-3868-7f90-b834-89c9b62e464f`, job `01a093e3-5d0b-7390-b7e9-db61d0024ce7`: `CANCEL_BEGIN` appeared, Stop transitioned the invocation to cancelled, `CANCEL_END` did not appear, and workspace ownership was released. This verifies agent-command cancellation; browser PTY intervention is a separate check.
- Browser PTY and preview acceptance uses an isolated **five-service** installation with its own normally bootstrapped owner account, disposable PostgreSQL, and an in-memory Temporal service. The first HTTP 500 in this fixture came from its missing Temporal dependency, which was added. It does not depend on the user's live browser login. Actual browser outcomes are recorded below, separately from fixture-based rendering checks.
- The loaded-history recovery API correction passes the 43-test journal/OpenAPI run above, including four bounded loaded-item regressions. Fresh live verification also passed **244 journal events / 69 items**, a forced disconnect at cursor **122**, and **5 bounded loaded-item recovery checks**.
- A new real agent run in chat `01a093fa-a372-7ad2-b387-e4fac702b30a` executed `cli.command.execute` exactly once using its original grants and `network=none`, returning stdout `42` and exit code 0.
- The final runner dependency and cookie-free Next.js corrections were built and activated. Both installations had zero active jobs before runner recreation. Final main web/gateway/tool-worker images were also activated after idle checks. The main installation reports **12 healthy services**; a fresh restart comparison matched **69 timeline items**, **7 file metadata/version records**, and downloaded bytes.
- The final document workflow retains pinned context, produces inspectable outputs, publishes `report.docx` v2 under its original artifact, and recovers its transcript and files after restart. Combined with the final authenticated browser cases below, this completes local release acceptance. Browser sessions were cleaned up, the isolated five-service Compose project was taken down, and disposable PostgreSQL was stopped. Retained volumes were preserved; the main installation remains healthy and running.

### Final authenticated browser acceptance

- A normal owner session passed CSV upload, a guarded editor save producing v2, working-disk browsing, PTY computation returning 42, reconnect, interrupt, and close. Static HTML counter interaction, preview isolation, and edited-source **Refresh from changes** also passed. This combined browser run completed in **17.1 seconds**.
- Vite hydration, source checkpoint refresh, and counter interaction passed through the browser in **30.6 seconds**.
- WebSocket acceptance initially exposed a missing production runner dependency. Adding `websockets>=15` to the runtime dependencies and lock fixed it. The production-only image loads Uvicorn's `websockets_sansio` implementation; the final browser test passed actual WebSocket echo and HTTP POST with both Cookie and Authorization absent at the preview application. WebSocket exceptions now close gracefully.
- Next.js initially failed in the opaque-origin iframe with a `document.cookie` `SecurityError`. The cookie-free bootstrap fixed hydration while preserving isolation. Actual Chromium checks confirmed empty cookie reads, inert cookie writes, `origin=null`, and blocked parent access. The final Next.js browser case passed hydration, received HMR frames, and HTTP POST.
- Proxy corrections cover Next.js trailing-slash redirects, internal-host redirects, authenticated canonical public-origin CSP, and rejection of redirect traversal outside the scoped mount. **33 tests**, strict mypy, Ruff, and an independent review passed before the successful final browser rerun.
- All **four final browser cases passed together in 1.4 minutes**: (1) Vite/React hydration, received HMR frames, and source-checkpoint refresh to counter 11; (2) Next.js cookie-free hydration, received HMR frames, and POST; (3) HTTP POST and browser WebSocket echo with Cookie/Authorization absent; (4) CSV/HTML upload, guarded CSV v2, directory browsing, PTY command returning 42, reconnect/interrupt/close, opaque HTML interaction/isolation, source-version **Refresh from changes**, and CSV persistence after page reload. Sessions were cleaned up.
- Final screenshots for all four cases were visually reviewed: no overflow or blocking overlays. Evidence is retained under `apps/web/test-results/live-final`.
- Provider login, service workers, and persistent preview cookies remain outside the selected initial profile, as documented in [the operations guide](../operations/agentic-workspace.md).

## Files and preview backend evidence

- Implemented migration 0045, saved project CRUD, conversation-owned file metadata, immutable versions, local content-addressed blobs, bounded text/image/PDF/CSV/XLSX/DOCX/PPTX extraction, uploads/downloads/previews, pinned model inputs, revision-aware editor saves, checkpoints/diffs/restoration, annotations, and branch file/input copies. Contracts: `agentic-files-contract.md`.
- Complex document parsing runs in a separate process with a 15-second wall limit and sanitized environment; Linux applies CPU/memory/descriptor limits. HTML is returned as code from managed-file previews. Source inputs are staged under immutable revision paths without overwriting edited files. `cli.file.publish` verifies actual runner bytes and stores a persistent snapshot.
- File/media/API/CLI publication and preview transport suite: **33 passed** on 2026-09-11. Includes retained branch attachments after source deletion, stale saves, cross-workspace/forged references, archive retention, restoration CAS, PDF/DOCX extraction, gateway revocation/origin checks, credential stripping, and bounded transport. CLI inventory/validator coverage: **2 passed**. Targeted Ruff checks passed; strict mypy covered 11 source files.
- Implemented scoped HTTP/WebSocket preview gateway and private runner transport, sandbox CSP on successful/error responses, cookie/credential stripping, opaque-origin capability verification, stable framework prefixes, and adapters for static/HTTP, Vite, and Next.
- Real Docker acceptance in `scripts/verify_agentic_previews.py`: static HTML, HTTP POST + WebSocket echo, **Vite 8.2.1** mounted page/module, and **Next 16.3.1** mounted page/chunk prefix/POST route all passed. The first three passed together; Next passed a subsequent `--next-only` run after fixing its generated disposable config. Test volumes/containers are uniquely named and cleaned up; the live runner's startup reaper is never invoked.
- Runtime findings fixed during acceptance: preview source copy must not preserve root-owned directory metadata; the shell must preserve the maintained document runtime PATH; disposable `/app` must permit native npm modules to map executable pages. Source remains read-only, container root remains read-only, capabilities remain dropped, and ports remain private.
- Project-source retention is fixed and tested: the project seeds a new chat after the original chat/file/checkpoint rows are deleted. Branch attachment copies also own their immutable revisions. Uncertain-job ownership, production output capture and compare-and-set lease release findings were addressed by the runtime integrator.
- Aggregate managed storage quota is enforced at the content-addressed store under a per-workspace cross-process lock. `JHIN_FILES_WORKSPACE_QUOTA_BYTES` defaults to **10 GiB / 10,737,418,240 bytes**. Identical blobs count once; archived/project/older/orphan bytes remain counted. New-byte publication returns 413 at the limit; existing reads remain available and no automatic deletion occurs.
- File operations and terminal closure now report timestamped disk measurements. Gateway persistence refuses stale generations/older measurements and preserves the distinction between complete and partial measurements. Storage/runtime checks, including concurrent quota refusal and offline backup/restore of an archived chat's two file versions, passed **30/30** in the targeted suite.
- The intermittent local Supabase MCP listing failure was traced to Composio's public toolkit slug entering the process-wide secret registry. Typed Composio bindings now omit only that validated public field from fragment registration; account identity and the whole encrypted-envelope plaintext remain scrubbed. The previously failing Composio→local-MCP order and regression checks pass **30/30**; the complete secret-store suite passes **14/14**.
- Authenticated browser terminal reconnect, static iframe interaction, Vite/Next.js HMR and refresh, HTTP POST, and production WebSocket transport all pass in the final combined run above. Their acceptance findings are fixed and local release acceptance is complete.

### Live-check corrections

- Reading DOCX through `cli.file.read` previously sent binary NUL characters into PostgreSQL JSONB, causing commit failures and uncertain tool outcomes. The text reader now validates UTF-8/NUL content in the sandbox before emitting a page, and rejects retained older binary responses with actionable document-library guidance. General tool payload sanitization replaces PostgreSQL-invalid NUL/surrogate characters in keys and values, with secret redaction before and after normalization.
- A real isolated PostgreSQL JSONB commit/replay regression passes for the original binary-output shape. Sanitizer, CLI file behavior, managed-file API and backup tests pass **164/164** in the broader focused run (**1 deselected**); targeted Ruff checks and strict mypy across 6 source files pass. After rebuilding, live `cli.file.read` returned one actionable `binary_file` refusal without a database retry, and the same-path document revision passed as recorded above.
- Direct editing of an upload before a first agent turn now checks whether its working path exists. A confirmed missing path is created with an atomic absent-file precondition; different existing bytes return a conflict. Pinned uploads remain unchanged, and existing artifact identity survives working-file edits.
- Download filenames now use the source path basename independently of the display title, preserving `.docx`, `.csv`, and other original extensions. Publication guidance explicitly tells agents to revise and republish the original output path for a new immutable version of the same artifact.
- Human working-disk growth admission now applies recorded per-disk/tenant limits with 507 refusals while retaining reads, export and recovery controls. It is not a hard cap on a running process's writes.
- `scripts/verify_agentic_chat.py` makes journal replay, review, project seeding, pre-restart baseline and post-restart recovery repeatable. It reads `JHIN_LIVE_API_KEY` from the process environment without printing the credential. Review/project phases intentionally mutate only the selected acceptance workflow.
- LAN HTTP support follows the existing API policy: local installations require `APP_ENV=dev`, `COOKIE_SECURE=false`, and `APP_URL` matching the actual LAN browser origin. Production/staging plaintext remains loopback-only; production HTTPS uses `COOKIE_SECURE=true`. This is documented configuration, not a transport-policy relaxation.


## Timeline, provider and runtime safety evidence

- Implemented versioned canonical conversation journal in 0046 with same-transaction snapshots and commit-ordered per-chat sequences, paginated item snapshots, authenticated SSE replay/recovery, optional NATS wakeups, and retained all-tool log downloads. Public projections omit private manifests, raw requests, credentials and hidden reasoning.
- Added typed OpenAI-compatible/Anthropic streaming, bounded tool fragment assembly, usage/citations, local image blocks, distinct generation attempts, split-secret draft protection and provider cancellation while idle. Terminal completion alone dispatches tools; failed partial streams do not dispatch effects.
- Added per-turn Ask/Plan/Act gateway enforcement, profile/context pinning, FIFO queued turns, queued edits/removal, steering delivery/consumption receipts, pause/resume/stop, project association and branch ancestry. Branches copy bounded visible history plus independent immutable workspace/input revisions without replaying tasks.
- Reviewed gateway/session authority and fixed stale disk fences, revoked tickets, one-shot claims after uncertain responses, reconnect sequence namespaces, retained-log duplication and session expiry/cleanup. Gateway also refuses human writes while prior command outcomes remain unconfirmed. Legacy failed/runner_error reconciliation now requires runner evidence and compares the previously observed status/error before closing a row.
- Verification: **30 owned source files pass strict mypy**, targeted Ruff checks pass; **54 Temporal recorded-history/failure tests pass**, **250 portable sandbox-runner tests pass with 1 skip**, **21 reconciliation/output tests pass**, and focused API/gateway/session/generation tests cover the new contracts. A broad Windows run produced 819 passes with remaining Unix socket/transport platform failures; the completed Linux sandbox/CLI result is recorded below. The initial full Temporal test-server run was interrupted; the subsequent complete workflow suite passed **154 tests** in 633 seconds.
- Final clean PostgreSQL base→0047 and 0047→0044→0047 round trip passed after amended 0045 project retention and Composio binding metadata classification, plus concurrent journal/rollback/output integration tests.
- The root integrator completed observability integration, local deployment, restart verification, and the combined browser/runtime acceptance recorded above.


Timeline/runtime final checkpoint: **668 passed, 5 skipped, 1 deselected** for the complete sandbox-runner + CLI suites against shared source in a disposable Linux container. This supersedes the earlier 638-pass checkpoint. Final amended migration chain passes clean base→0047 plus 0047→0044→0047, including project source retention and Composio binding metadata classification; PostgreSQL journal concurrency/rollback/output tests pass 2/2. Public terminal projection boundary tests pass 5/5. No live database or existing service was changed by the timeline subagent.
